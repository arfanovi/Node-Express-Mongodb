sudo: false
language: node_js

node_js:
  - 10

install:
  - travis_retry npm install
script:
  - npm run test:single

jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      node_js: lts/*
      script:
          - npm run semantic-release 
